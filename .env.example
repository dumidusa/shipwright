# Shared secret used to sign/verify the /deploy webhook (HMAC-SHA256)
DEPLOY_WEBHOOK_SECRET=change-me-to-a-long-random-string


# Full image ref the deployer should pull, without the tag
DEMO_IMAGE=ghcr.io/YOUR_GH_USERNAME/mini-paas-demo
